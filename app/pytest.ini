[pytest]
# Suite du composant app (navigateur Gallica) : logique backend du serveur local.
testpaths = tests
# Par défaut : uniquement les tests hermétiques (aucun réseau/subprocess réel).
addopts = -m "not integration"
markers =
    integration: nécessite réseau/curl réel vers Gallica — exclu du run par défaut
